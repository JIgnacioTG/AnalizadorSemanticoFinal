	MOV a, 2
	MOV b, 3
	MOV AH, 2
	MOV AL, b
	ADD AH, AL
	MOV a, AH
	MOV AH, a

Condicion1:
	MOV AL, 1
	CMP AH, AL
	JE Instruccion1
	JMP Condicion2

Instruccion1:
	MOV AL, 2
	MOV BL, b
	ADD AL, BL
	MOV a, AL
	JMP SalidaN50

Condicion2:
	MOV AL, 2
	CMP AH, AL
	JE Instruccion2
	JMP Condicion3

Instruccion2:
	MOV AL, 2
	MOV BL, b
	ADD AL, BL
	MOV a, AL
	JMP SalidaN50

Condicion3:
	MOV AL, 3
	CMP AH, AL
	JE Instruccion3
	JMP Condicion4

Instruccion3:
	MOV AL, b

Condicion4:
	MOV BL, 1.2
	CMP AL, BL
	JE Instruccion4
	JMP Condicion5

Instruccion4:
	MOV BL, 1.2
	MOV DH, b
	ADD BL, DH
	MOV b, BL
	JMP SalidaN50

Condicion5:
	MOV BL, 2
	CMP AL, BL
	JE Instruccion5
	JMP Condicion6

Instruccion5:
	MOV BL, 2
	MOV DH, b
	ADD BL, DH
	MOV b, BL
	JMP SalidaN50
	MOV b, 3

SalidaN50:
	FIN